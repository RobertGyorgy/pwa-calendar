# What's next

1. **Home-page action notification** — show a badge/notice on `/dashboard` when patients need subscription renewal or other actions.
2. **Custom renewal UI/CSS** — replace the `window.prompt` with an in-app sheet/buttons for choosing the new session package.
3. **New-patient sheet scroll** — stop the sheet from jumping down when typing the name (keyboard focus issue).
4. **Patient delete** — verify it no longer reloads the page; if it still does, debug the swipe-to-delete click handler.
5. **Fluid page loading** — add entry transitions / reduce skeleton flash on tab switches.
6. **Calendar drag polish** — confirm moving a patient no longer nudges the whole screen.
